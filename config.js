window.MOTION_DIRECTOR_CONFIG = {
  // If the frontend is hosted on GitHub Pages, set this to your secure backend URL.
  // Example: "https://motion-director-api.vercel.app"
  // If the site and API are deployed together on Vercel, leave it empty.
  BACKEND_URL: ""
};
